This are just short notes on env
# Rake file
 - A rake file is what contains all our tasks
 - check rake tasks => $ bundle exec rake -T

# create a db
 - bundle exec rake db:create

# create a table
 - 